{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "externalbalancer-operator.fullname" .) .Values.serviceAccount.name }}
  namespace: {{ include "externalbalancer-operator.namespace" . }}
{{- end }}
